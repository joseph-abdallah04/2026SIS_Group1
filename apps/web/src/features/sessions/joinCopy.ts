/** Shared copy for a code that does not resolve to a joinable session. */
export const UNKNOWN_JOIN_TITLE = "Can't join that session";
export const UNKNOWN_JOIN_BODY =
  'That code is wrong, or the session does not exist. Check the code and try again.';
