{
  "name": "roundtable",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "deepmerge-ts": "^8.0.0"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --workspace @roundtable/server\" \"npm run dev --workspace @roundtable/web\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run db:deploy --workspace @roundtable/server && node apps/server/dist/index.js",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:nuke": "docker compose down -v"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "concurrently": "^10.0.5",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2"
  },
  "dependencies": {
    "react-router-dom": "^7.18.2"
  }
}
